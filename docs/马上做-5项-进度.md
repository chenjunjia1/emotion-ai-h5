# 马上做 5 项 — 进度（2026-05-24）

## 已由助手完成

| # | 事项 | 状态 |
|---|------|------|
| 1 | 本地 `.env.local` 域名改为 `www.emovalue.top` | ✅ |
| 1 | 重新生成 `.env.vercel-paste`（15 条，已复制剪贴板） | ✅ |
| 2 | Supabase 你的账号 `13798221796` → `role=admin` | ✅ 已在数据库执行 |
| 2 | 代码：登录时白名单手机号自动同步 admin | ✅ `v1.ts`（需再部署一次才上线） |
| 5 | `layout.tsx` 默认域名改为 `www.emovalue.top` | ✅（需再部署） |

## 你需要在 Vercel 点 3 下（约 5 分钟）

1. **emotion-ai-h5-v2** → **Environment Variables**
2. **Paste .env** → **Ctrl+V**（剪贴板已备好）→ 三个环境全选 → **Save**  
   或只 Edit 这两条：
   - `NEXT_PUBLIC_APP_URL` = `https://www.emovalue.top`
   - `ALIPAY_NOTIFY_URL` = `https://www.emovalue.top/api/pay/notify/alipay`
3. **Deployments** → **⋯ → Redeploy**

## 你需要自己删旧项目（约 2 分钟）

1. 打开 **emotion-ai-h5-v2** → **Settings → Domains**，确认有 `emovalue.top` / `www.emovalue.top`
2. 打开旧项目 **emotion-ai-h5** → **Settings** → 最底 **Delete Project**

## 第 4 项：真机自测清单

- [ ] https://www.emovalue.top 登录 `13798221796`
- [ ] https://www.emovalue.top/admin 能进
- [ ] 选题盲盒生成一次
- [ ] 内容库有记录

验证码（dev 模式）：Vercel → **Logs** 搜 `[sms:dev]` 或 `sms_logs`

## 代码更新部署（admin 自动同步 + 默认域名）

GitHub 上传最新 `src`（含 `src/lib/server/db/v1.ts`、`src/app/layout.tsx`）→ Vercel 自动部署或手动 Redeploy。
